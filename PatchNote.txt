func.py
1) 276 строка marks = '''()'",'''  добавляет символы в переменную
2)283 создан цикл заменяющий все символы из marks в переменной m на пустые символы
Аналогично с переменной n
причиной вывода в виде ('текст',) является то, что из БД текст преобразовывался в string, так как вывод в Телеграмме только в string

причиной того, что не отображаются ответы в некоторых случаях (first_count= 1   second_count= 1). строки 298 297
До этого было 0, то есть, на ноль делить нельзя, заменил на 1


Для установки всех зависимостей в терминал ввести
pip install pyTelegramBotAPI
pip install sqlite3

P.S bot.py строка 30